# 更新履歴

Kiriha の利用者に関係する変更をバージョンごとにまとめています。日付はリリース日です。

更新は自動で適用されます（起動時と 6 時間おきにチェック）。手動で確認したいときは「… > 更新の確認」を使ってください。

## v1.0.61 — 2026-08-06

- お気に入りの上に「現在のフォルダーを追加」「並べ替え」のアイコンを並べ、背景を右クリックしなくても同じ操作ができるようにしました
- お気に入りが項目でいっぱいになると背景を右クリックできる場所が無くなっていたので、一覧の下に余白を空けました
- お気に入りの中にグループ分け用のフォルダーを作る機能をやめました。実在のフォルダーと見分けが付きにくいためです（すでに作ってあるフォルダーはそのまま表示されます）
- お気に入りの項目を左端に詰め、クイックアクセスと同じ位置から並ぶようにしました。横スクロールバーは出さず、幅に収まらない名前は末尾を「…」で省略します
- ステータスバーからドライブの空き領域を外し、代わりに 1 件だけ選んでいるときはその更新日時を表示するようにしました。サイズはこれまでどおり選択情報に出ます（複数選択なら合計、更新日時は出ません）
- 選択にフォルダーが含まれるときは、フォルダーの中身も数えて合計サイズに含めるようにしました。集計はバックグラウンドで進み、大きなフォルダーでは途中経過が「…」付きで更新されます（ジャンクションやショートカットの参照先はたどりません）
- 別のタブを見ている間に、そのタブで選択していたファイルが他のアプリなどで削除されると、タブに戻ったときに一覧から消えているのに「1 個の項目を選択」の表示が残り、その状態で切り取りや削除を実行できてしまうのを修正しました
- リンク先が無くなったお気に入りをクリックしたとき、これまでは何も起きませんでした。Windows のショートカットと同じように「リンク先が見つかりません。このお気に入りを削除しますか？」と確認し、OK で削除できるようにしました

## v1.0.60 — 2026-08-06

- ファイル一覧を一番下までスクロールしても、最後の 1 行が横スクロールバーに隠れて最後まで見えなかったのを修正しました
- フォルダーの中身が変わったときに一覧全体を作り直していたのをやめ、増えた・減った・変わった項目だけをその場で更新するようにしました。更新と重なったタイミングでクリックやダブルクリックが効かなくなる、スクロール位置が動く、アイコンが一瞬消える、といったことが起きにくくなります
- お気に入りへドラッグしている間に、左のタブ一覧が反応して仮のタブが現れることがあったのをやめました
- アドレスバーに `C:` のようなドライブ名だけを入力したときに、ドライブ直下ではなく別のフォルダーが開いてしまうのを修正しました。フォルダー名だけを入力した場合も、エクスプローラーと同じく表示中のフォルダーからの相対として開きます
- お気に入りへドラッグしたときに、どの項目の間に入るのか（フォルダーの中に入る場合はどのフォルダーか）が線と枠で分かるようになり、落とした位置にそのまま追加されるようになりました。既に登録済みのお気に入りを落とした場合はその位置へ移動します

## v1.0.59 — 2026-08-06

- ごみ箱への削除を Ctrl+Z で元に戻そうとして確認ダイアログをキャンセルした場合に、その後 Ctrl+Z を押しても二度と戻せなくなっていたのを修正しました
- 画像の回転がファイルのロック等で時間切れになった直後にもう一度回転すると、遅れて終わっていた書き込みと重なって意図せず 2 回分回ってしまうことがあったのを防止しました
- 切り取り（Ctrl+X）した直後にクリップボードへの書き込みが一部失敗すると、切り取り表示のまま実際は貼り付けるとコピーになってしまう、または以前コピーしていた内容が消えてしまうことがあったのを修正しました
- コマンドラインやスクリプトから相対パス（例: `.`）を指定して 2 個目の Kiriha を起動すると、既に開いているウィンドウが意図しないフォルダーを開くことがあったのを修正しました
- 起動直後に複数のフォルダーをほぼ同時に開いた場合、一部が開かれずに終わることがあったのを修正しました
- ライセンスの有効性をオンラインで確認する際、サーバー側の一時的な応答不備を「失効した」と誤って判定し、購入済みのライセンスが試用切れ扱いになりうる不具合を修正しました
- 依存パッケージを更新しました

## v1.0.58 — 2026-08-05

- 起動してから最初の右クリックで、「ターミナルで開く」「Code で開く」など一部のアプリの項目がメニューに出ないことがあったのを修正しました（起動時に先読みするようにしました）

## v1.0.57 — 2026-08-05

- 画面上部のお気に入りバーをやめ、左ペインの表示切り替えを「クイックアクセス / ツリー表示 / お気に入り」の 3 種類にしました。これまで登録していたお気に入りはそのまま「お気に入り」に表示されます（フォルダー分けした階層も畳んだり開いたりできます）
- お気に入りの項目に、★ではなく実際のアイコン（フォルダーやファイルの種類に応じたもの）を出すようにしました。設定のアイコンセット（元のアイコン / Material / Windows）にも追従します。お気に入りの中でグループ分けに使うフォルダーだけは 📁 のままです
- お気に入りに登録したファイルをクリックすると、関連付けされたアプリで開くようにしました（フォルダーはこれまでどおりそのタブで移動します）
- お気に入りの操作は今までどおりです。クリックで移動、中クリックでバックグラウンドの新しいタブ、右クリックで名前の変更や削除、余白の右クリックで現在のフォルダーの追加・並べ替え、ペインへドラッグ＆ドロップで登録できます。Ctrl+D での追加と Ctrl+Shift+B（お気に入り表示の切り替え）も引き続き使えます
- 設定画面の「お気に入りバーを表示する」と、右上メニューの「お気に入りバー」は左ペインの切り替えに置き換わったため削除しました
- 検索ボックスやアドレスバーを右クリックしたときのメニュー（切り取り・コピー・貼り付け）が英語のままだったのを、表示言語に合わせて出るようにしました
- 一つ上の階層へ戻ったとき、いま出てきたフォルダーを選択した状態にし、そのフォルダーが一覧の中央に見えるようスクロールするようにしました（どこから上がってきたのか見失わなくなります）
- ファイル一覧で文字を打つと、その文字で始まる項目へ移動するようにしました（エクスプローラーと同じで、「S」「O」と続けて打てば SoftwareDistribution のように打った文字列の先頭一致で探します。1 秒以上あけると打ち直しになり、同じ文字を続けて打つと次の候補へ送ります）
- 切り取り・コピー・パスのコピー・リンクのコピーを実行したとき、ドラッグ中の表示と同じ見た目のトーストで結果を知らせるようにしました（ステータスバーを隠していても操作できたことが分かります）
- ファイル一覧にフォーカスがある状態で Ctrl+Shift+C を押すと、パスのコピーではなく通常のコピーになっていたのを修正しました
- フォルダー内で何かが更新されるたびにファイル一覧がちらつく（アイコンやサムネイルが一瞬消えて出直す）現象を解消しました。表示中の内容が変わっていない項目はそのまま残し、実際に変わったファイルだけを更新します。選択やスクロール位置、パンくずの表示も維持されます

## v1.0.56 — 2026-08-05

- ツリー表示でフォルダーを右クリックできるようにしました（開く / 新しいタブで開く / お気に入りに追加 / 新しいファイル・フォルダー / 切り取り・コピー・貼り付け / パスのコピー / 名前の変更 / 削除 / エクスプローラーで開く / 更新・すべて折りたたむ / プロパティ、および Windows 標準メニューへの「その他のオプションを確認」）
- ツリー表示のヘッダーにある「新しいファイル」などのボタンを右寄せにしました
- 切断されたネットワークドライブなどが 1 台あっても、PC（ドライブ一覧）と左ペインのドライブ一覧が開けるようにしました（問題のあるドライブだけ容量表示を省いて残します）
- 設定とフォルダー別の表示設定の保存を、置き換えができない環境（クラウド同期フォルダーなど）でも失敗しない書き込み方式にそろえました
- ライセンスの失効確認中に別のキーを認証した場合でも、有効なキーが取り消されないようにしました

## v1.0.55 — 2026-08-05

- ギャラリーでの画像回転中に強制終了や電源断が起きても、元の画像ファイルが壊れないようにしました（ライセンス認証情報の保存についても同様です）
- クリップボード経由でのファイルの切り取り・コピー・貼り付けで、非常に長いパス（深い階層のクラウド同期フォルダーなど）が正しく扱えるようにしました
- ツリー表示の階層をアドレスバーのパスと一致させました。ルートが「PC」になり、そこからドライブ → 実際のフォルダーへとそのまま辿ります（例: PC › Windows (C:) › Users › IMT › Desktop）。以前の「デスクトップ」を頂点にして「マイ ドキュメント」「マイ コンピュータ」を並べる構成は廃止しました
- サイドバーのツリー表示に、VSCode のエクスプローラーと同じ 4 つのボタン（新しいファイル / 新しいフォルダー / 最新の情報に更新 / すべて折りたたむ）を追加しました。新規作成はツリーの中に入力欄が現れるインライン方式で、「a/b/c.txt」のように入力すると中間フォルダーごと作成できます。使えない名前や重複は入力欄の下にその場で表示されます
- その右端に Visual Studio のソリューションエクスプローラーと同じ「アクティブ ドキュメントとの同期」トグルを追加しました。オン（既定）の間はツリーが現在のフォルダーへ自動で追従し、オンに切り替えた瞬間にも現在地まで展開します。オフにすると追従しません
- ツリーの内容は自動では更新されなくなりました。「最新の情報に更新」ボタンで再読み込みします（展開状態と選択は維持されます）
- パンくずリスト（PC › ドライブ › フォルダー…）をアドレスバーからステータスバーの左側へ移動しました。動作はこれまでのアドレスバーと同じです（幅が足りないときは浅い階層から畳んで「…」ボタンにまとめ、各階層の「›」からサブフォルダーへ移動できます）
- アドレスバーは Web ブラウザと同じく、現在のフォルダーのパスをそのまま文字で表示するようになりました。クリックして直接入力する操作はこれまでどおりです

## v1.0.54 — 2026-08-03

- パスのコピーを 2 つのボタンに分けました。アドレスバー右のボタンは今開いているフォルダーのパス専用になり、選択したファイルのパスをコピーするボタンをツールバー（コピーの右隣）へ戻しました
- ツールバーのボタンはファイルやフォルダーを選んでいるときだけ押せます。Ctrl+Shift+C はこれまでどおり、選択が無ければ現在のフォルダーのパスをコピーします

## v1.0.53 — 2026-08-02

- アドレスバー右の「パスをコピー」ボタンが、ファイルを選んでいなくても押せるようになりました。何も選んでいないときは現在のフォルダーのパスをコピーします（Ctrl+Shift+C も同じ動作です）

## v1.0.52 — 2026-08-01

- アドレスバーをクリック（または Ctrl+L / F4）したときに、現在のパスを自動でクリップボードへコピーしなくなりました。コピーしたいパスが上書きされず、そのまま貼り付けられます
- 「選択のパスをコピー」ボタンをツールバーからアドレスバーの右へ移動しました
- 現在のフォルダーのパスをコピーしたいときは、これまでどおりアドレスバーを右クリック →「アドレスのコピー」を使ってください

## v1.0.51 — 2026-08-01

- 「PC」の並べて表示に、ドライブの使用率ゲージと「空き領域 180 GB/475 GB」を表示するようにしました（エクスプローラーと同じ見た目です）
- ドライブ名をエクスプローラーと同じ表記に揃えました。ボリュームラベルを付けていないドライブは「ローカル ディスク (C:)」のように種別名で表示されます
- 容量の表記が TB まで対応しました（これまでは「1,905.34 GB」のように GB 止まりでした）
- 「PC」のドライブの並びを、常にドライブレター順（C: → D: …）にしました



- 「PC」（ドライブ一覧）は並べて表示に固定しました。表示メニューの表示方法と Ctrl+ホイールでは変わりません（フォルダーへ戻ると、そのフォルダーの表示設定がそのまま効きます）
- ツールバーの「削除」の右に「プロパティ」ボタンを追加しました
- 右クリックメニュー（Kiriha 独自）の上部アイコン行に「パスのコピー」と「プロパティ」を移動しました。コピーの右がパスのコピー、削除の右がプロパティです
- アクリル効果の使用時に、設定画面の背景だけ不透明で浮いていたのを修正しました（ファイル一覧と同じ濃さになります）
- 表示方法に「並べて表示」を追加しました。エクスプローラーと同じタイル表示で、48px のアイコンまたはサムネイルの右に 名前・種類・サイズ を並べます（名前が 2 行に折り返したときはサイズを省略、フォルダーは名前のみ）。表示メニュー・設定の既定表示・Ctrl+ホイールのどれからでも選べます
- 右クリックメニューを Windows 11 風の自前メニューへ刷新。アイコン並びの行（切り取り / コピー / 名前の変更 / 共有 / 削除）と厳選した項目を並べ、送る などのシェルのサブメニューは「その他のオプションを確認」へまとめました。設定 > 表示 の「右クリックメニュー」から、Kiriha 独自（Windows 11 風）/ Kiriha 独自（シェルの項目をそのまま）/ Windows 標準 の 3 方式を選べます
- 「PC」でドライブを選んだまま削除ボタンや Delete キーを押すと、ドライブの中身の削除が始まってしまう問題を修正
- ドライブを誤操作から守るため、切り取り・コピー・削除・名前の変更をすべて無効にしました（ボタンも右クリックメニューもグレー表示になり、Ctrl+X / Ctrl+C / Delete / F2 も効きません）。ドライブのパスをコピーする「選択のパスをコピー」はこれまでどおり使えます
- ドライブをドラッグできないようにしました。「PC」の一覧・左ペイン・フォルダーツリーのどこから掴んでも動きません（ドライブへファイルをドロップする操作はこれまでどおり使えます）

## v1.0.49 — 2026-07-31

- 買い切りライセンスを ¥980 から ¥1,980 へ改定しました（購入済みのライセンスはそのまま有効です）

## v1.0.48 — 2026-07-31

- 右クリックメニューから開いたプログラムの作業フォルダーが、対象のフォルダーになるよう修正（「管理者として実行」で `C:\Windows\System32` が使われ、同じフォルダーのファイルを相対パスで参照するバッチが失敗していた）
- 複数ファイルをドラッグしたときに同じファイルが重複して渡ることがある問題を修正（WinMerge に 2 ファイル落とすと 3 ペインになる等）

## v1.0.47 — 2026-07-30

- 起動直後に一部のフォルダーやファイルで Windows 標準アイコンが表示されず、F5 を押すまで空欄のままだった不具合を修正

## v1.0.46 — 2026-07-30

- Ctrl+Z で削除を取り消せるようになりました（ごみ箱から元の場所へ戻します）
- タブの並べ替えやピン留めでタブが増殖する不具合を修正

## v1.0.45 — 2026-07-29

- マテリアルアイコンテーマを選んでいるとフォルダーを開けない不具合を修正
- ドラッグ中にタブへ 1 秒重ねるとそのタブへ切り替わるようになりました（別のタブで開いているフォルダーへドロップできます）
- ドラッグ中のプレビュータブへドロップすると無視される不具合を修正
- ツリー表示ボタンの見た目をタブと同じチップ表現に統一
- 選択中の垂直タブの色を、ダーク系テーマで少し明るく調整

## v1.0.44 — 2026-07-29

- 右ボタンでのドラッグ＆ドロップに対応。ドロップ時に「ここにコピー / ここに移動 / ショートカットをここに作成 / キャンセル」を表示します（エクスプローラーと同じ）
- 詳細表示の「サイズ」列の既定幅を 90 → 100 px へ拡大

## v1.0.43 — 2026-07-28

- アドレスバーのパンくずが幅に収まらないとき、浅い階層側から畳んで左端の「…」から開けるようになりました（以前は現在のフォルダー側が右へはみ出して押せなかった）
- ツールバーに「選択のパスをコピー」ボタンを追加
- 詳細表示の「サイズ」列の既定幅を 180 → 90 px へ変更

## v1.0.42 — 2026-07-27

- ギャラリーでシークバーにフォーカスがあるとき、↑ / ↓ がガンマ補正ではなく再生位置の移動になっていた不具合を修正

## v1.0.41 — 2026-07-27

- 動画の鮮鋭化とガンマ補正を GPU 処理へ移行し、再生中の CPU 負荷をほとんど増やさずに掛けられるようにしました
- 最大化・全画面のときはウィンドウ枠まわりの余白・角丸・輪郭を落とし、映像が画面の端まで届くようにしました
- ギャラリー中の Ctrl+← / Ctrl+→ でファイル送り、↑ / ↓ でガンマ補正
- 画像・動画のダブルクリックでギャラリーの全画面表示に入る設定を追加（既定 ON、Windows の関連付けは変更しません）。各社の RAW も画像として扱います

## v1.0.40 — 2026-07-27

- 動画のフレーム転送上限を引き上げ、フル HD を等倍で表示できるようにしました（以前は縮小してから引き伸ばしていたためぼやけていた）
- ギャラリーの静止画を表示サイズちょうどでデコードし、最後に鮮鋭化を掛けるようにしました。設定 > 表示 で ON / OFF と強さ（弱め / 標準 / 強め / 最強）を選べます
- ギャラリーの操作をコントロールバー 1 本へ集約し、全画面表示・終了・前後送りを追加
- ギャラリー中もアドレスバーを表示し、全画面表示のときだけ畳むようにしました
- フォルダーの右クリックに「エクスプローラーで開く」を追加。Kiriha 自身のメニューからは「Kiriha で開く」を除きました
- 垂直タブ列の左右の余白をそろえ、ステータスバー非表示時の空き帯を解消

## v1.0.39 — 2026-07-27

- アクリル効果を無効にしたとき、ウィンドウの地とアドレスバー帯がテーマ色より明るい灰色になっていた問題を修正
- アクリル効果を有効にしたときの透明度を上げ、壁紙のぼかしがより見えるよう調整
- 垂直タブ列の境界に出ていた区切り線を非表示に（幅を掴んで変える操作はそのまま）

## v1.0.38 — 2026-07-27

- 配色テーマ「Dim」を追加（紺寄りのダーク）
- アクリル効果を有効にしたとき、ぼかし部分だけ壁紙の色に染まって不透明な面から浮く問題を修正
- 選択中の垂直タブが背景と同化していた問題を修正
- 設定に「ライセンス認証を解除」を追加（購入は有効なまま、同じメールアドレスで再認証できます）

## v1.0.37 — 2026-07-26

- ライセンス認証をメールアドレス + 6 桁の確認コード方式に変更しました。ライセンスキーの入力も引き続き使えます（設定画面とロック画面の両方に同じ導線があります）
- PNG の無劣化回転に対応（スクリーンショットで回転ボタンが押せなかった問題を解消）
- ギャラリーで前後 1 枚を先読みし、ホイール送りの待ち時間をなくしました
- 詳細表示に横スクロールバーを追加し、列ヘッダーもスクロールに追従するようにしました
- 設定に「フォルダーの既定表示（表示方法・並べ替え）」と「フォルダーごとの表示設定をリセット」を追加
- 詳細表示の列幅もフォルダーごとに記憶するようになりました
- 垂直タブバーの最小幅を 180 → 96 へ緩和
- ギャラリーのコントロールバーを薄くし、倍率表示を 100% へ戻すボタンへ統合

## v1.0.36 — 2026-07-26

- ギャラリーの操作を、メイン表示とフィルムストリップの間の共通コントロールバーへ集約
- 拡大縮小を表示領域の中央基準に修正し、100% へ戻すボタンを追加
- メイン画像で手のひらツールのカーソル（開いた手 / 握った手）を表示
- 動画の再生速度をドロップダウン化し、速度・音量・ミュートを設定へ保存するようにしました
- 動画の回転を表示だけの回転として実装（ファイルは書き換えません）
- フィルムストリップの高さの下限を 90 → 54 へ緩和
- 設定画面で表示言語の注記がドロップダウンに重なる問題を修正

## v1.0.35 — 2026-07-26

- ギャラリーに JPEG / TIFF の無劣化回転を追加（画質を落とさずに向きだけを書き換えます）
- ギャラリーのプレビューで拡大縮小とドラッグ移動ができるようになりました
- 動画のコントロールバーを、ポインタを動かしている間だけ表示するようにしました
- 動画のシークがドラッグ後に十数秒待たされる問題を解消
- 回転時にファイルへの書き込みが固まる問題を解消
- Exif の向きを Kiriha 側の表示にも反映するようにしました

## v1.0.34 — 2026-07-26

- UI を 17 言語に対応しました。設定画面のドロップダウンから即時に切り替えられます。初回起動時は Windows の表示言語から自動判定します
- ギャラリーで動画を再生できるようになりました（再生 / 一時停止・シーク・音量・ループ・速度変更に対応）
- 動画の表示間隔がばらついてカクついていた問題を修正
- 動画のフレームが更新されなくなることがある不具合を修正
- 範囲選択と一覧表示の内部処理を軽量化

## v1.0.33 — 2026-07-26

- `C:/x` と `C:\x` のように書き方が違うだけの同じフォルダーが、別フォルダーとして扱われることがある問題を修正
- ファイルサイズの表示をエクスプローラーと同じ切り捨てに変更し、桁区切りを表示するようにしました（1,024 KB のような紛らわしい表示を解消）
- 設定ファイルが読めないときにバックアップから復旧するようにし、設定が黙って初期値に戻る問題を修正
- 終了時にフォルダーごとの表示設定が保存されないことがある問題を修正

## v1.0.32 — 2026-07-25

- ファイル操作をエクスプローラー本体と同じ仕組みへ移行し、コピー中に別のコピー・移動・削除を開始できるようになりました（以前は「別のファイル操作が完了するまでお待ちください」で拒否されていた）
- 長いパス（260 文字超）の制約も解消されました
- 設定タブの「今すぐ更新を確認」が無反応だった不具合を修正
- クラウド同期ドライブ上の画像を見ているとき、読み取りが瞬断するとアプリが落ちる不具合を修正

## v1.0.31 — 2026-07-24

- ステータスバーのアイコンサイズスライダーを上下中央へ配置し、フォーカスを受けないようにしました
- ギャラリーのメイン画像エリア全体（レターボックス含む）でホイール送りが効くようになりました
- タブの固定・並べ替えでタブ一覧とギャラリーの情報パネルが同時表示される問題を修正
- サムネイル読み込みを刷新。フォルダーを移動すると前のフォルダー分を即座に打ち切り、低解像度 → 表示解像度の 2 段階で表示するようにしました
- サムネイル読み込みでクリックが待たされないようにしました

## v1.0.30 — 2026-07-24

- ギャラリーのメイン画像をクリックしたあとも ← / → で前後の画像へ移動できるようにしました

## v1.0.29 — 2026-07-24

- ギャラリー表示中は、タブ一覧の場所に選択中の画像の Exif・メタ情報パネル（種類・寸法・サイズ・撮影日時・カメラ・レンズ・F 値・露出・ISO 等）を表示するようにしました
- フィルムストリップの高さを境界ドラッグで調節できるようになりました（全タブ共通で保存されます）
- メイン画像上のホイールで前後の画像へ切り替え、フィルムストリップ上のホイールは横スクロールになりました
- メイン画像の右クリックで通常のファイルと同じ右クリックメニューを表示
- メイン画像をクリックしたあとも Delete / Shift+Delete / F2 / Ctrl+X・C・V が効くようにしました
- 画像クリックで選択が外れ、ギャラリーが閉じたように見える問題を修正

## v1.0.28 — 2026-07-24

- ライセンスロック中はキーボード操作も含めて操作領域全体を無効化するようにしました
- ロック画面のカードがアクリル効果使用時に透けていた問題を修正
- 認証済みのときは設定のライセンス欄から購入導線とキー入力欄を隠すようにしました
- ライセンスの失効照会に予備の経路を追加

## v1.0.27 — 2026-07-24

- ライセンスの購入・照会の窓口を Sekisho hub へ一本化
- クイックアクセスの表示がエクスプローラーと一致しないことがある不具合を修正

## v1.0.26 — 2026-07-24

- ライセンスを署名付きキー方式（オフライン検証）へ全面移行
- 試用 14 日の終了後は、認証するまで全画面ロックを表示するようにしました
- 起動時に失効リストを照会し、オンライン確認ができないまま 30 日経つとロックするようにしました

## v1.0.25 — 2026-07-24

- ギャラリーモードを追加。アイコンサイズのスライダーを最大にすると 1 枚表示 + フィルムストリップに切り替わります（Esc・✕・スライダーで復帰）
- 買い切りライセンス（試用 14 日）を導入しました
- パスのコピー時にアドレスバー直下へ通知を表示するようにしました

## v1.0.24 — 2026-07-24

- どの再読み込み（F5・並べ替え・絞り込み・フォルダー監視の自動更新・右クリックメニュー実行後）でも選択が解除されないようにしました
- 複数選択のまま右クリックしても選択が維持され、メニューの操作が選択全体に適用されるようにしました
- 特殊フォルダーが混ざった複数選択でプロパティが表示されない問題を修正
- クラシックメニューがアプリのテーマに追従するようになりました
- 詳細表示の選択色をアクセント色の塗りに変更

## v1.0.23 — 2026-07-23

- 設定を追加: ツリーのドラッグ / ドロップ禁止、タブ・フォルダー背景のダブルクリック / ホイールクリック動作
- 詳細表示で、選択・ホバー・ドラッグの判定を名前列に限定し、日時〜サイズの領域は範囲選択に使えるようにしました
- 切断中のネットワークドライブやシェル拡張の不調による固まりに対する堅牢性を改善
- 一覧の読み込みと選択処理を高速化

## v1.0.22 — 2026-07-23

- ツールバーの共有ボタンから Windows 標準の共有シートを開けるようになりました
- JXL / AVIF / HEIC のサムネイルとプレビューに対応
- 「種類」「ツール」「コマンドパレット」を削除して UI を整理し、「オプション」を … メニューからツールバーの歯車ボタンへ移動
- 一覧の再読み込みが重なるとアイコンやサムネイルが欠けることがある問題を修正

## v1.0.21 — 2026-07-23

- タイトルバーを 42px → 32px に薄型化
- 隠し属性 + システム属性の項目（My Music 等の互換用フォルダー）をエクスプローラーと同様に非表示化
- ファイル操作後の一覧再読み込みが何度も走らないようにしました
- コンパクトビューをアプリ全体の設定として全タブへ即時反映するようにしました
- 切断したドライブがあるときのフリーズを防止

## v1.0.20 — 2026-07-23

- 左ペインにフォルダーツリー表示を追加（現在地の自動展開・選択同期）。表示切替ボタンをツールバーに追加しました
- フォルダーをタブバーへドロップすると新しいタブとして挿入できるようになりました（仮配置のプレビュー付き）。既存タブの並べ替えもライブ反映に統一
- 検索ボックスに入力するだけでサブフォルダーを含む再帰検索を行うようになりました
- コンパクトビュー・ステータスバー表示・プレビューペイン幅を保存するようにしました

## v1.0.19 — 2026-07-22

- 表示設定を保存していないフォルダーへ移動したとき、直前のフォルダーの並べ替えが引き継がれる問題を修正

## v1.0.18 — 2026-07-22

- アイコンセットを選べるようになりました（絵文字 / マテリアルアイコンテーマ / Windows 標準）。左ペインのアイコンも追従します
- 右クリックメニュー経由のファイル操作後に、一覧の表示残りを解消するようにしました

## v1.0.17 — 2026-07-22

- フォルダーごとの表示設定とアドレスバー操作を改善

## v1.0.16 — 2026-07-22

- 更新ダイアログのタイトルを統一

## v1.0.15 — 2026-07-22

- 存在しないフォルダーを開こうとしたときに「PC」へ戻るようにしました

## v1.0.14 — 2026-07-21

- ダイアログと範囲選択を改善

## v1.0.13 — 2026-07-21

- スタートメニューの登録先を修正

## v1.0.12 — 2026-07-21

- サムネイル表示とファイル一覧を改善

## v1.0.11 — 2026-07-20

- 垂直タブの背景と配信設定を改善

## v1.0.10 — 2026-07-15

- アイコン表示・ダイアログ・アンインストール処理を改善

## v1.0.9 — 2026-07-15

- 固定タブが階層を維持するようにし、垂直タブの UI を改善
- 複数選択のドラッグプレビューとドロップ先の強調表示を追加
- 右クリックメニューとクリップボード連携を改善

## v1.0.8 — 2026-07-15

- 前回終了時のモニターへウィンドウ位置を復元するようにしました
- ダークテーマのタブ配色とアクティブ下線を調整

## v1.0.7 — 2026-07-14

- 横 / 垂直タブの切り替えと幅の保存、ドラッグでの並べ替えを追加
- 「並べて表示」を Windows 標準の列方向レイアウトへ調整
- クイックアクセスとファイル一覧のドラッグ操作を修正

## v1.0.6 — 2026-07-14

- 固定タブと通常タブの非選択時の背景を統一し、ダーク系の配色を整理

## v1.0.5 — 2026-07-14

- アイコン表示を仮想化し、大量のファイルがあるフォルダーの表示と並べ替えを高速化

## v1.0.4 — 2026-07-14

- 最後に選択していたタブを次回起動時に復元するようにしました
- タブ復元時に設定が壊れることがある不具合を修正
- サブモニターで全画面表示していたウィンドウが、再起動後にメインモニターで最大化される不具合を修正
- Ctrl+X / C / V・Delete・F2 がファイル一覧以外にフォーカスがあるときに効かない不具合を修正
- コピー中に別のファイル操作を行うとエラーになる不具合を修正
- タスクトレイ格納で起動したときに自動更新チェックがスキップされる不具合を修正し、6 時間おきの定期チェックを追加

## v1.0.3 — 2026-07-13

- クラッシュをログに残すようにしました
- 起動時のタブ復元・ドラッグ中・左ペイン構築での固まりを解消（オフラインのネットワークドライブ対策）
- 作成日時での並べ替えがメニューから選べなかった問題を修正
- 危険な引数でのバッチ実行を拒否するなど、セキュリティを強化
- 固定タブに薄い背景を追加し、タブのドラッグをカーソル追従のゴースト表示に変更

## v1.0.2 — 2026-07-13

- 自動更新の配信元を Cloudflare R2 へ移行
- 外部プログラムの起動を、信頼できる実行ファイルのパスに限定するようにしました
- フォルダー監視をタブ間で共有し、同じフォルダーを複数タブで開いても重複しないようにしました

## v1.0.1 — 2026-07-12

- ランディングページの配信を修正

## v1.0.0 — 2026-07-12

- Kiriha 初回リリース。Chrome ライクなタブ型ファイルマネージャー、Windows シェル互換のファイル操作、Material Icon Theme に対応
